{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @chikitsa360/web exec prisma generate && turbo build --filter=@chikitsa360/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "regions": ["sin1"],

  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
